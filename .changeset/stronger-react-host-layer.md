---
"use-mcp-react": minor
---

Add the optional `use-mcp-react/apps` subpath with MCP Apps capability advertisement, authorized `ui://` resource loading, iframe host rendering, and upstream app bridge lifecycle handling.
